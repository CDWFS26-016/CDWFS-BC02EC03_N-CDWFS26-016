{% extends "base.html.twig" %}

{% block title %}Relais et Châteaux{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1>🏰 Relais et Châteaux</h1>
            <p class="text-muted mb-3">Réservez et commenter pour 580 établissements dans 65 pays</p>
        </div>
        {% if is_granted('ROLE_RESPONSABLE') or is_granted('ROLE_ADMIN') %}
            <a href="{{ path('event_create') }}" class="btn btn-primary">+ Ajouter un établissement</a>
        {% endif %}
    </div>

    {% if events|length == 0 %}
        <div class="alert alert-info">
            Aucun établissement pour le moment.
        </div>
    {% else %}
        <div class="row">
            {% for event in events %}
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">{{ event.title }}</h5>
                            <p class="card-text text-muted">
                                <small>
                                    📅 {{ event.date|date('d/m/Y H:i') }}
                                </small>
                            </p>
                            <p class="card-text">
                                <small>
                                    👤 Responsable: {{ event.responsable.email }}
                                </small>
                            </p>
                            <p class="card-text">
                                <small>
                                    💬 {{ event.reviews|filter(r => r.isValidated)|length }} avis
                                </small>
                            </p>
                        </div>
                        <div class="card-footer bg-white">
                            <a href="{{ path('event_show', {'id': event.id}) }}" class="btn btn-sm btn-outline-primary">
                                Voir les détails
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}
